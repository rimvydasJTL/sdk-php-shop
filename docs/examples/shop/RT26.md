# (RT26) Mark a return as compliant or non compliant

```php
use Mirakl\MMP\Shop\Client\ShopApiClient;
use Mirakl\MMP\Shop\Request\Returns\UpdateReturnsComplianceRequest;

$api = new ShopApiClient('API_URL', 'API_KEY', 'SHOP_ID');
$request = new UpdateReturnsComplianceRequest();
$result = $api->updateReturnsCompliance($request);
```
